package service

// The proximity-expansion fallback tests were replaced by domain-pool tests
// in domain_pool_test.go (domainCodes, DomainGeneralist). This file exists so
// the deletion of nearbySpecialties is visible in git history.
